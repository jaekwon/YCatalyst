setup:
> db.invites.save({'_id': 'invitecode1'})
> db.records.save({'_id': 'rootrootroot', 'comment': 'the root'})

npm install: 
// - xml2js -> actually, i forked on github here: git@github.com:jaekwon/node-xml2js.git
- jsdom // buggy but usable
- i'm sure missing a lot
